// Stub — filled in Task 9 (SPA-2 T9: Admin Registros table)
import { AppLayout } from "@/components/layout/AppLayout";

export function AdminRegistrosPage() {
  return (
    <AppLayout title="Registros (Admin)">
      <div />
    </AppLayout>
  );
}
