# Captura v2 — Campos completos + Vista de equipo — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Añadir 4 campos de captura (sexo, edad, estructura, observación) y exponer al capturista con un filtro "míos / equipo", abriendo lectura a COORDINADOR, sobre el módulo de activistas ya desplegado.

**Architecture:** El scoping jerárquico ya vive en `registro_service._role_scoped` (un LIDER ya recibe a su equipo). El trabajo es aditivo: 4 columnas nullable en `registros` (migración 0013), exponer `activista_nombre` + un param `scope`, separar el guard de lectura/escritura del router, y actualizar el formulario y la lista en el frontend. No se tocan crypto, consentimiento, ARCO, retention ni exports.

**Tech Stack:** FastAPI · SQLAlchemy 2.0 (Mapped) · Alembic · Pydantic v2 · pytest (SQLite in-memory) · React + TypeScript + Vite + Tailwind · Vitest.

## Global Constraints

- Reglas Alembic (CLAUDE.md): valores de enum en MAYÚSCULAS; DDL idempotente con guardas; `ALTER TYPE … ADD VALUE` fuera de transacción; compatible SQLite. Esta migración es **solo `add_column`** de columnas nullable — sin enums nuevos.
- Golden Rules: toda query filtra por `organization_id` (ya lo hace `scoped_query`); `organization_id`/`activista_id` en writes vienen del JWT, nunca del input; endpoints devuelven schemas Pydantic, nunca ORM crudo; RBAC en la capa API vía `require_roles`; la clave de elector **nunca** se loguea ni se expone en listas — solo `clave_masked`.
- Migración head actual: `0012`. La nueva es `0013`, `down_revision = "0012"`.
- Tests backend: `cd backend && python3 -m pytest`. `FERNET_KEY` y `RATE_LIMIT_ENABLED=false` los pone `conftest.py`.
- Frontend: `cd frontend && npm run build` (type-check) y `npm run test` (vitest).
- Orden de campos en el formulario (pedido del usuario): nombre → sexo → clave de elector → edad → sección → domicilio → colonia → teléfono → estructura → observación.

---

## File Structure

**Backend**
- `backend/app/models/registro.py` — +4 columnas (`sexo`, `edad`, `estructura`, `observacion`).
- `backend/alembic/versions/0013_captura_v2.py` — **crear**; migración aditiva idempotente.
- `backend/app/schemas/registro.py` — Create/Update/Read + validadores + `activista_nombre`.
- `backend/app/services/registro_service.py` — persistir campos; `scope`; resolver `activista_nombre` en lote.
- `backend/app/routers/registros.py` — separar guard lectura/escritura; pasar `scope`.
- `backend/tests/test_registro_captura_v2.py` — **crear**; tests de la feature.

**Frontend**
- `frontend/src/api/registros.ts` — tipos + `listMisRegistros(scope, q)`.
- `frontend/src/modules/captura/CapturaPage.tsx` — form (4 campos), toggle scope, badge capturista.

---

## Task 1: Modelo + migración 0013 (4 columnas nuevas)

**Files:**
- Modify: `backend/app/models/registro.py`
- Create: `backend/alembic/versions/0013_captura_v2.py`
- Test: `backend/tests/test_registro_captura_v2.py`

**Interfaces:**
- Produces: columnas ORM `Registro.sexo: Optional[str]`, `Registro.edad: Optional[int]`, `Registro.estructura: Optional[str]`, `Registro.observacion: Optional[str]`.

- [ ] **Step 1: Write the failing test**

Crear `backend/tests/test_registro_captura_v2.py`:

```python
"""Captura v2 — nuevos campos + vista de equipo (activista_nombre, scope, coordinador read)."""
from app.models.registro import Registro


def test_registro_model_has_captura_v2_columns():
    cols = set(Registro.__table__.columns.keys())
    assert {"sexo", "edad", "estructura", "observacion"}.issubset(cols)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && python3 -m pytest tests/test_registro_captura_v2.py::test_registro_model_has_captura_v2_columns -v`
Expected: FAIL — `AssertionError` (columnas ausentes).

- [ ] **Step 3: Add the columns to the model**

En `backend/app/models/registro.py`, tras la línea de `area` (`area: Mapped[Optional[str]] = mapped_column(String(120), nullable=True)`), añadir:

```python
    sexo: Mapped[Optional[str]] = mapped_column(String(1), nullable=True)          # "M" | "F"
    edad: Mapped[Optional[int]] = mapped_column(Integer, nullable=True)
    estructura: Mapped[Optional[str]] = mapped_column(String(120), nullable=True)
    observacion: Mapped[Optional[str]] = mapped_column(String(1000), nullable=True)
```

Añadir `Integer` a la importación de sqlalchemy al inicio del archivo (la línea `from sqlalchemy import (...)`), insertándolo en orden alfabético:

```python
from sqlalchemy import (
    Boolean, DateTime, Float, ForeignKey, Index, Integer, LargeBinary, String, UniqueConstraint,
)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && python3 -m pytest tests/test_registro_captura_v2.py::test_registro_model_has_captura_v2_columns -v`
Expected: PASS.

- [ ] **Step 5: Write the migration**

Crear `backend/alembic/versions/0013_captura_v2.py` (sigue el patrón idempotente de `0011_arco.py`; solo `add_column`, columnas nullable, seguro en SQLite y PG):

```python
"""Captura v2: sexo, edad, estructura, observacion en registros.

Revision ID: 0013
Revises: 0012
Create Date: 2026-07-02

Notes
-----
* Migración puramente aditiva: 4 columnas nullable. Sin enums nuevos.
* Idempotente: guarda _column_exists en cada add_column.
* SQLite-safe: ADD COLUMN simple, sin batch (no hay constraints).
* La columna existente ``area`` se conserva intacta (no se toca).
"""
from __future__ import annotations

import sqlalchemy as sa
from alembic import op

revision = "0013"
down_revision = "0012"
branch_labels = None
depends_on = None

_NEW_COLUMNS = (
    ("sexo", sa.String(length=1)),
    ("edad", sa.Integer()),
    ("estructura", sa.String(length=120)),
    ("observacion", sa.String(length=1000)),
)


def _column_exists(table: str, column: str) -> bool:
    bind = op.get_bind()
    insp = sa.inspect(bind)
    if not insp.has_table(table):
        return False
    return any(c["name"] == column for c in insp.get_columns(table))


def upgrade() -> None:
    for name, coltype in _NEW_COLUMNS:
        if not _column_exists("registros", name):
            op.add_column("registros", sa.Column(name, coltype, nullable=True))


def downgrade() -> None:
    for name, _ in reversed(_NEW_COLUMNS):
        if _column_exists("registros", name):
            op.drop_column("registros", name)
```

- [ ] **Step 6: Verify the migration is the new head and round-trips**

Run: `cd backend && python3 -m alembic heads`
Expected: muestra `0013 (head)`.

Run: `cd backend && python3 -m alembic history | head -3`
Expected: `0013` aparece con `down_revision 0012`.

> Nota: los tests usan `Base.metadata.create_all` (no Alembic), así que las columnas ya existen en SQLite por el modelo. La migración es para PostgreSQL prod; se valida con `alembic heads`/`history` (no requiere una BD PG local).

- [ ] **Step 7: Run the full backend suite (no regression)**

Run: `cd backend && python3 -m pytest -q`
Expected: PASS (baseline previo + el nuevo test).

- [ ] **Step 8: Commit**

```bash
git add backend/app/models/registro.py backend/alembic/versions/0013_captura_v2.py backend/tests/test_registro_captura_v2.py
git commit -m "feat(captura-v2): modelo + migración 0013 — sexo/edad/estructura/observacion"
```

---

## Task 2: Schemas Pydantic (Create/Update/Read + validadores)

**Files:**
- Modify: `backend/app/schemas/registro.py`
- Test: `backend/tests/test_registro_captura_v2.py`

**Interfaces:**
- Consumes: nada de tareas previas (solo el modelo, ya existente).
- Produces:
  - `RegistroCreate` acepta `sexo: Optional[str]` (∈ {"M","F"}), `edad: Optional[int]` (0..120), `estructura: Optional[str]` (≤120), `observacion: Optional[str]` (≤1000).
  - `RegistroUpdate` acepta los mismos 4 campos, todos opcionales.
  - `RegistroRead` expone `sexo`, `edad`, `estructura`, `observacion`, `activista_nombre: Optional[str] = None`.

- [ ] **Step 1: Write the failing tests**

Añadir a `backend/tests/test_registro_captura_v2.py`:

```python
import pytest
from pydantic import ValidationError

from app.schemas.registro import RegistroCreate, RegistroRead


def test_registro_create_accepts_new_fields():
    m = RegistroCreate(
        nombre_completo="Ana Ruiz", consentimiento=True,
        sexo="F", edad=34, estructura="Red Norte", observacion="Interesada en salud",
    )
    assert m.sexo == "F" and m.edad == 34
    assert m.estructura == "Red Norte" and m.observacion == "Interesada en salud"


def test_registro_create_rejects_bad_sexo():
    with pytest.raises(ValidationError):
        RegistroCreate(nombre_completo="X Y", consentimiento=True, sexo="X")


@pytest.mark.parametrize("edad", [-1, 121])
def test_registro_create_rejects_edad_out_of_range(edad):
    with pytest.raises(ValidationError):
        RegistroCreate(nombre_completo="X Y", consentimiento=True, edad=edad)


def test_registro_read_has_activista_nombre_default_none():
    fields = RegistroRead.model_fields
    assert "activista_nombre" in fields
    assert {"sexo", "edad", "estructura", "observacion"}.issubset(fields)
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd backend && python3 -m pytest tests/test_registro_captura_v2.py -k "new_fields or bad_sexo or edad_out or activista_nombre" -v`
Expected: FAIL — `RegistroCreate` no acepta `sexo`/`edad` (extra ignorado → asserts fallan) y `RegistroRead` no tiene los campos.

- [ ] **Step 3: Add fields + validators to the schemas**

En `backend/app/schemas/registro.py`:

Añadir un validador de sexo reutilizable tras `_validate_clave`:

```python
def _validate_sexo(v: Optional[str]) -> Optional[str]:
    if v is None or v == "":
        return None
    cleaned = v.strip().upper()
    if cleaned not in ("M", "F"):
        raise ValueError("sexo must be 'M' or 'F'")
    return cleaned
```

En `RegistroCreate`, tras el campo `area`, añadir:

```python
    sexo: Optional[str] = Field(default=None)
    edad: Optional[int] = Field(default=None, ge=0, le=120)
    estructura: Optional[str] = Field(default=None, max_length=120)
    observacion: Optional[str] = Field(default=None, max_length=1000)
```

y tras el validador `_clave` de `RegistroCreate`:

```python
    @field_validator("sexo")
    @classmethod
    def _sexo(cls, v):
        return _validate_sexo(v)
```

Repetir los mismos 4 campos y el validador `_sexo` en `RegistroUpdate` (tras su campo `area` y su validador `_clave`).

En `RegistroRead`, tras la línea `area: Optional[str]`, añadir:

```python
    sexo: Optional[str]
    edad: Optional[int]
    estructura: Optional[str]
    observacion: Optional[str]
    activista_nombre: Optional[str] = None
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `cd backend && python3 -m pytest tests/test_registro_captura_v2.py -k "new_fields or bad_sexo or edad_out or activista_nombre" -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/app/schemas/registro.py backend/tests/test_registro_captura_v2.py
git commit -m "feat(captura-v2): schemas Create/Update/Read + validadores sexo/edad + activista_nombre"
```

---

## Task 3: Servicio persiste los campos nuevos

**Files:**
- Modify: `backend/app/services/registro_service.py`
- Test: `backend/tests/test_registro_captura_v2.py`

**Interfaces:**
- Consumes: `RegistroCreate`/`RegistroUpdate` con los 4 campos (Task 2).
- Produces: `create_registro`/`update_registro` persisten `sexo`, `edad`, `estructura`, `observacion`; el endpoint POST/GET los devuelve.

- [ ] **Step 1: Write the failing test (API-level)**

Añadir a `backend/tests/test_registro_captura_v2.py`:

```python
from tests.conftest import ALPHA_CAMPAIGN_ID, auth_headers


def _hdr(client, email, campaign_id=ALPHA_CAMPAIGN_ID):
    h = auth_headers(client, email)
    h["X-Campaign-Id"] = campaign_id
    return h


def test_create_persists_new_fields(client):
    h = _hdr(client, "activista1@alpha.gov")
    resp = client.post("/api/registros", json={
        "nombre_completo": "Ana Ruiz", "consentimiento": True,
        "sexo": "F", "edad": 34, "estructura": "Red Norte",
        "observacion": "Interesada en salud",
    }, headers=h)
    assert resp.status_code == 201, resp.text
    body = resp.json()
    assert body["sexo"] == "F" and body["edad"] == 34
    assert body["estructura"] == "Red Norte"
    assert body["observacion"] == "Interesada en salud"
    client.delete(f"/api/registros/{body['id']}", headers=h)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `cd backend && python3 -m pytest tests/test_registro_captura_v2.py::test_create_persists_new_fields -v`
Expected: FAIL — el body no contiene `sexo`/`edad` (el service no los guarda; salen `None`).

- [ ] **Step 3: Persist the fields in the service**

En `backend/app/services/registro_service.py`, dentro de `create_registro`, en la construcción de `Registro(...)`, tras `area=data.area,` añadir:

```python
        sexo=data.sexo,
        edad=data.edad,
        estructura=data.estructura,
        observacion=data.observacion,
```

`update_registro` ya aplica los campos genéricamente vía `data.model_dump(exclude_unset=True)` + `setattr`, así que los 4 campos nuevos se persisten automáticamente en updates. No requiere cambios ahí.

- [ ] **Step 4: Run test to verify it passes**

Run: `cd backend && python3 -m pytest tests/test_registro_captura_v2.py::test_create_persists_new_fields -v`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add backend/app/services/registro_service.py backend/tests/test_registro_captura_v2.py
git commit -m "feat(captura-v2): persistir sexo/edad/estructura/observacion en create_registro"
```

---

## Task 4: Vista de equipo — `activista_nombre`, `scope`, y lectura para COORDINADOR

**Files:**
- Modify: `backend/app/services/registro_service.py`
- Modify: `backend/app/routers/registros.py`
- Test: `backend/tests/test_registro_captura_v2.py`

**Interfaces:**
- Consumes: `_role_scoped(ctx)` (ya existente); `RegistroRead.activista_nombre` (Task 2).
- Produces:
  - `list_registros(db, ctx, q, limit, offset, scope="team")` — nuevo param `scope: str`. Con `scope="mine"` restringe a `Registro.activista_id == ctx.user.id`. Adjunta `row.activista_nombre` a cada fila.
  - Router: guard de **lectura** incluye COORDINADOR; guard de **escritura** sin cambios; `list_mine` acepta `?scope=mine|team`.

- [ ] **Step 1: Write the failing tests**

Añadir a `backend/tests/test_registro_captura_v2.py`:

```python
def test_lider_sees_team_with_activista_nombre(client):
    ha1 = _hdr(client, "activista1@alpha.gov")
    r = client.post("/api/registros", json={
        "nombre_completo": "Persona de A1", "consentimiento": True}, headers=ha1)
    rid = r.json()["id"]

    hl = _hdr(client, "lider@alpha.gov")
    lst = client.get("/api/registros/mios", headers=hl)
    assert lst.status_code == 200, lst.text
    row = next(x for x in lst.json()["items"] if x["id"] == rid)
    assert row["activista_nombre"] == "Alpha Activista 1"

    client.delete(f"/api/registros/{rid}", headers=ha1)


def test_scope_mine_excludes_team(client):
    ha1 = _hdr(client, "activista1@alpha.gov")
    r = client.post("/api/registros", json={
        "nombre_completo": "Solo de A1", "consentimiento": True}, headers=ha1)
    rid = r.json()["id"]

    hl = _hdr(client, "lider@alpha.gov")
    team = client.get("/api/registros/mios", params={"scope": "team"}, headers=hl)
    mine = client.get("/api/registros/mios", params={"scope": "mine"}, headers=hl)
    team_ids = {x["id"] for x in team.json()["items"]}
    mine_ids = {x["id"] for x in mine.json()["items"]}
    assert rid in team_ids          # el líder ve al activista en 'team'
    assert rid not in mine_ids      # pero no en 'mine' (no es suyo)

    client.delete(f"/api/registros/{rid}", headers=ha1)


def test_coordinador_can_read_but_not_write(client):
    hc = _hdr(client, "coord@alpha.gov")
    # lectura permitida (200)
    assert client.get("/api/registros/mios", headers=hc).status_code == 200
    # escritura denegada (403)
    assert client.post("/api/registros", json={
        "nombre_completo": "Coord no captura", "consentimiento": True},
        headers=hc).status_code == 403
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `cd backend && python3 -m pytest tests/test_registro_captura_v2.py -k "team_with_activista or scope_mine or coordinador_can_read" -v`
Expected: FAIL — `activista_nombre` es `None`; `scope` no filtra; COORDINADOR recibe 403 en lectura (excluido del guard actual).

- [ ] **Step 3: Add `scope` + `activista_nombre` to the service**

En `backend/app/services/registro_service.py`, reemplazar la firma y el cuerpo de `list_registros` por:

```python
def list_registros(
    db: Session, ctx: CampaignContext, q: Optional[str],
    limit: int, offset: int, scope: str = "team",
) -> tuple[list[Registro], int]:
    stmt = _role_scoped(ctx)
    if scope == "mine":
        stmt = stmt.where(Registro.activista_id == ctx.user.id)
    if q:
        like = f"%{q}%"
        stmt = stmt.where(
            or_(Registro.nombre_completo.ilike(like), Registro.seccion.ilike(like))
        )
    total = db.execute(select(func.count()).select_from(stmt.subquery())).scalar_one()
    rows = (
        db.execute(
            stmt.order_by(Registro.created_at.desc()).limit(limit).offset(offset)
        )
        .scalars()
        .all()
    )
    rows = list(rows)
    # Resolve capturer names in one query (no N+1). activista_nombre is a
    # transient attribute read by RegistroRead (from_attributes); never mapped.
    ids = {r.activista_id for r in rows if r.activista_id}
    names: dict[str, str] = {}
    if ids:
        for uid, fname in db.execute(
            select(User.id, User.full_name).where(User.id.in_(ids))
        ).all():
            names[uid] = fname
    for r in rows:
        r.activista_nombre = names.get(r.activista_id)
    return rows, total
```

(`User` y `select` ya están importados en el módulo.)

- [ ] **Step 4: Split the read/write guards + pass `scope` in the router**

En `backend/app/routers/registros.py`:

Añadir `Literal` a la importación de typing al inicio: `from typing import Annotated, Literal, Optional`.

Reemplazar la definición de `CapturaCtx` (líneas ~20) por dos guards:

```python
# Escritura: activistas, capturistas, líderes, admins (COORDINADOR se excluye a
# propósito — usa la consola, no captura). Superadmins auto-pasan.
CapturaWriteCtx = Annotated[object, Depends(require_roles(
    UserRole.ACTIVISTA, UserRole.CAPTURISTA, UserRole.LIDER, UserRole.ADMIN))]
# Lectura: lo mismo + COORDINADOR (ve a su estructura; el scoping real lo impone
# _role_scoped en el service).
CapturaReadCtx = Annotated[object, Depends(require_roles(
    UserRole.ACTIVISTA, UserRole.CAPTURISTA, UserRole.LIDER,
    UserRole.COORDINADOR, UserRole.ADMIN))]
```

En cada endpoint, sustituir el parámetro `_perm: CapturaCtx`:
- `create` → `_perm: CapturaWriteCtx`
- `list_mine` → `_perm: CapturaReadCtx`
- `get_one` → `_perm: CapturaReadCtx`
- `update` → `_perm: CapturaWriteCtx`
- `delete` → `_perm: CapturaWriteCtx`

En `list_mine`, añadir el query param `scope` y pasarlo al service:

```python
@router.get("/registros/mios", response_model=RegistroList)
def list_mine(
    db: DbSession, ctx: CampaignCtx, _perm: CapturaReadCtx,
    q: Annotated[Optional[str], Query()] = None,
    scope: Annotated[Literal["mine", "team"], Query()] = "team",
    limit: Annotated[int, Query(ge=1, le=200)] = 50,
    offset: Annotated[int, Query(ge=0)] = 0,
) -> RegistroList:
    rows, total = registro_service.list_registros(db, ctx, q, limit, offset, scope)
    return RegistroList(items=[RegistroRead.model_validate(r) for r in rows],
                        total=total, limit=limit, offset=offset)
```

- [ ] **Step 5: Run the new tests + the existing registro suites**

Run: `cd backend && python3 -m pytest tests/test_registro_captura_v2.py tests/test_registro_permissions.py tests/test_registros.py -v`
Expected: PASS. En particular `test_viewer_cannot_read_or_write_registros` sigue en 403 (VIEWER no está en ningún guard) y `test_capture_cycle_and_consent` sigue verde.

- [ ] **Step 6: Run the full backend suite (no regression)**

Run: `cd backend && python3 -m pytest -q`
Expected: PASS.

- [ ] **Step 7: Commit**

```bash
git add backend/app/services/registro_service.py backend/app/routers/registros.py backend/tests/test_registro_captura_v2.py
git commit -m "feat(captura-v2): vista de equipo — activista_nombre + scope mine/team + lectura COORDINADOR"
```

---

## Task 5: Frontend — tipos API + campos nuevos en el formulario

**Files:**
- Modify: `frontend/src/api/registros.ts`
- Modify: `frontend/src/modules/captura/CapturaPage.tsx`

**Interfaces:**
- Consumes: endpoint `POST /registros` que acepta `sexo/edad/estructura/observacion` (Task 3).
- Produces:
  - Tipos `Registro` y `RegistroCreate` con `sexo`, `edad`, `estructura`, `observacion` (+ `Registro.activista_nombre`).
  - `listMisRegistros(scope?, q?)`.
  - `FormState` del form con los 4 campos y su envío en el payload.

- [ ] **Step 1: Extend the API client types**

En `frontend/src/api/registros.ts`:

En `interface Registro`, tras `area: string | null;` añadir:

```typescript
  sexo: string | null;
  edad: number | null;
  estructura: string | null;
  observacion: string | null;
  activista_nombre: string | null;
```

En `interface RegistroCreate`, tras `area?: string;` añadir:

```typescript
  sexo?: string;
  edad?: number;
  estructura?: string;
  observacion?: string;
```

Reemplazar `listMisRegistros`:

```typescript
export async function listMisRegistros(
  scope: "mine" | "team" = "team",
  q?: string,
): Promise<RegistroList> {
  const { data } = await apiClient.get<RegistroList>("/registros/mios", {
    params: { scope, ...(q ? { q } : {}) },
  });
  return data;
}
```

- [ ] **Step 2: Verify the type-check fails**

Run: `cd frontend && npm run build`
Expected: FAIL — `CapturaPage.tsx` llama `listMisRegistros` sin args compatibles / `useAsync(listMisRegistros, [])` con nueva firma; y (tras Step 3) referencias a campos nuevos aún no existen. (Este build rojo confirma que los tipos se propagan; se corrige en los pasos siguientes.)

- [ ] **Step 3: Add the 4 fields to the form**

En `frontend/src/modules/captura/CapturaPage.tsx`:

En `interface FormState`, añadir tras `area: string;`:

```typescript
  sexo: string;      // "" | "M" | "F"
  edad: string;      // string en el input, se castea al enviar
  estructura: string;
  observacion: string;
```

En `EMPTY_FORM`, añadir:

```typescript
  sexo: "",
  edad: "",
  estructura: "",
  observacion: "",
```

En `handleSubmit`, dentro de `const payload = { ... }`, tras la línea de `area`, añadir el envío condicional:

```typescript
      ...(form.sexo && { sexo: form.sexo }),
      ...(form.edad.trim() && { edad: Number(form.edad) }),
      ...(form.estructura.trim() && { estructura: form.estructura.trim() }),
      ...(form.observacion.trim() && { observacion: form.observacion.trim() }),
```

Añadir los controles al grid del formulario **en el orden pedido**. Sexo va justo después de "Nombre completo" (que ocupa `sm:col-span-2`), luego la clave sube antes de edad. Reordenar así el bloque de campos dentro del `<div className="grid grid-cols-1 gap-4 sm:grid-cols-2">`:

1. Nombre completo (`sm:col-span-2`) — sin cambios.
2. **Sexo** (nuevo) — botones segmentados:

```tsx
            {/* Sexo */}
            <div>
              <span className="field-label">Sexo</span>
              <div className="mt-1 flex gap-2">
                {(["M", "F"] as const).map((s) => (
                  <button
                    key={s}
                    type="button"
                    onClick={() =>
                      setForm((p) => ({ ...p, sexo: p.sexo === s ? "" : s }))
                    }
                    className={`flex-1 rounded-lg border px-3 py-2 text-sm font-semibold transition-colors ${
                      form.sexo === s
                        ? "border-accent bg-accent/10 text-accent"
                        : "border-line text-ink-muted hover:border-accent/40"
                    }`}
                  >
                    {s === "M" ? "Masculino" : "Femenino"}
                  </button>
                ))}
              </div>
            </div>
```

3. **Edad** (nuevo):

```tsx
            {/* Edad */}
            <div>
              <label htmlFor="cap-edad" className="field-label">Edad</label>
              <input
                id="cap-edad"
                type="text"
                inputMode="numeric"
                className="field-input"
                placeholder="Años"
                value={form.edad}
                onChange={(e) =>
                  setForm((p) => ({
                    ...p,
                    edad: e.target.value.replace(/\D/g, "").slice(0, 3),
                  }))
                }
              />
            </div>
```

4. Clave de elector — mover el bloque existente aquí (sigue con `sm:col-span-2`).
5. Sección, 6. Teléfono, 7. Dirección (`sm:col-span-2`), 8. Colonia — bloques existentes, sin cambios de contenido.
9. **Estructura** (nuevo — reemplaza en la UI al viejo campo "Área / Programa"; borrar el bloque de `cap-area`):

```tsx
            {/* Estructura */}
            <div>
              <label htmlFor="cap-estructura" className="field-label">Estructura</label>
              <input
                id="cap-estructura"
                type="text"
                className="field-input"
                placeholder="Red o estructura"
                value={form.estructura}
                onChange={(e) =>
                  setForm((p) => ({ ...p, estructura: e.target.value }))
                }
              />
            </div>
```

10. **Observación** (nuevo, `sm:col-span-2`):

```tsx
            {/* Observación */}
            <div className="sm:col-span-2">
              <label htmlFor="cap-observacion" className="field-label">Observación</label>
              <textarea
                id="cap-observacion"
                rows={2}
                className="field-input resize-y"
                placeholder="Notas u observaciones"
                value={form.observacion}
                onChange={(e) =>
                  setForm((p) => ({ ...p, observacion: e.target.value }))
                }
              />
            </div>
```

> Nota: se elimina de la UI el input `cap-area` (Área/Programa). No se toca el backend `area`; simplemente el formulario ya no lo captura. `form.area` sale de `FormState`/`EMPTY_FORM`/payload.

- [ ] **Step 4: Update the list call sites to the new signature**

En `CapturaPage.tsx`, el `useAsync(listMisRegistros, [])` debe pasar el scope. Como `scope` se introduce en la Task 6, por ahora fija `team` para roles con equipo. Cambiar temporalmente a:

```tsx
  const registrosState = useAsync(() => listMisRegistros("team"), []);
```

(En Task 6 esto pasa a depender del toggle.)

- [ ] **Step 5: Verify the build passes**

Run: `cd frontend && npm run build`
Expected: PASS (type-check + Vite build sin errores).

- [ ] **Step 6: Run the offline unit tests (no regression)**

Run: `cd frontend && npm run test`
Expected: PASS — el payload encolado incluye los campos nuevos porque `RegistroCreate` los define y `sync.ts` reenvía el payload verbatim.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/api/registros.ts frontend/src/modules/captura/CapturaPage.tsx
git commit -m "feat(captura-v2): formulario con sexo/edad/estructura/observacion (reemplaza área)"
```

---

## Task 6: Frontend — toggle "míos / equipo" + badge del capturista

**Files:**
- Modify: `frontend/src/modules/captura/CapturaPage.tsx`

**Interfaces:**
- Consumes: `listMisRegistros(scope, q)` (Task 5); `perfilState.data.role`; `Registro.activista_nombre`.
- Produces: UI de la lista con toggle de scope (solo roles con equipo) y badge del nombre del capturista por fila.

- [ ] **Step 1: Add scope state gated by role**

En `CapturaPage.tsx`, tras los `useState` existentes, añadir:

```tsx
  const [scope, setScope] = useState<"mine" | "team">("team");
```

Tras `const perfilState = useAsync(getPerfil, []);`, derivar si el usuario tiene equipo:

```tsx
  const teamRoles = ["LIDER", "COORDINADOR", "ADMIN", "SUPERADMIN"];
  const hasTeam = Boolean(
    perfilState.data && teamRoles.includes(perfilState.data.role),
  );
```

Cambiar la carga de registros para que dependa del scope efectivo (un activista sin equipo siempre "mine"):

```tsx
  const effectiveScope = hasTeam ? scope : "mine";
  const registrosState = useAsync(
    () => listMisRegistros(effectiveScope),
    [effectiveScope],
  );
```

- [ ] **Step 2: Render the toggle in the list header**

En la `<Card title="Personas registradas" ...>`, reemplazar el prop `action` para incluir el toggle cuando `hasTeam`:

```tsx
        action={
          <div className="flex items-center gap-2">
            {hasTeam && (
              <div className="flex rounded-lg border border-line p-0.5 text-xs">
                {(["mine", "team"] as const).map((s) => (
                  <button
                    key={s}
                    type="button"
                    onClick={() => setScope(s)}
                    className={`rounded-md px-2.5 py-1 font-semibold transition-colors ${
                      scope === s
                        ? "bg-accent/10 text-accent"
                        : "text-ink-muted hover:text-ink"
                    }`}
                  >
                    {s === "mine" ? "Míos" : "Todo el equipo"}
                  </button>
                ))}
              </div>
            )}
            <span className="pill border-line text-ink-muted">
              {registros.length} total
            </span>
          </div>
        }
```

- [ ] **Step 3: Show the capturer badge in `PersonRow`**

Cambiar la firma de `PersonRow` para recibir `showActivista: boolean` y renderizar el badge. En `PersonRowProps` añadir `showActivista: boolean;`. En el `.map`, pasar `showActivista={hasTeam}`:

```tsx
              <PersonRow
                key={r.id}
                registro={r}
                index={i}
                showActivista={hasTeam}
                onDelete={handleDelete}
              />
```

Dentro de `PersonRow`, en el bloque `<div className="mt-1 flex flex-wrap ...">` de metadatos, añadir al inicio de los spans:

```tsx
          {showActivista && registro.activista_nombre && (
            <span className="font-semibold text-accent">
              {registro.activista_nombre}
            </span>
          )}
```

Y añadir `showActivista` a la desestructuración de props: `function PersonRow({ registro, index, showActivista, onDelete }: PersonRowProps)`.

- [ ] **Step 4: Verify the build passes**

Run: `cd frontend && npm run build`
Expected: PASS.

- [ ] **Step 5: Run the offline unit tests (no regression)**

Run: `cd frontend && npm run test`
Expected: PASS.

- [ ] **Step 6: Manual smoke (optional but recommended)**

Run: `cd frontend && npm run dev` (con el backend en `:8000`).
Verificar como `lider@alpha.gov`: aparece el toggle "Míos / Todo el equipo"; en "Todo el equipo" las filas de activistas muestran el badge con su nombre; como `activista1@alpha.gov` no aparece toggle ni badges.

- [ ] **Step 7: Commit**

```bash
git add frontend/src/modules/captura/CapturaPage.tsx
git commit -m "feat(captura-v2): toggle míos/equipo + badge del capturista en la lista"
```

---

## Self-Review (autor del plan)

**Spec coverage:**
- §2 Campos (sexo/edad/estructura/observacion) → Task 1 (modelo+migración), Task 2 (schemas), Task 3 (persistencia), Task 5 (form). ✓
- §2 `area` se conserva en DB, sale del form → Task 1 no la toca; Task 5 elimina el input. ✓
- §3.1 `activista_nombre` → Task 2 (schema) + Task 4 (resolución en lote). ✓
- §3.2 filtro scope mine/team → Task 4 (backend) + Task 6 (toggle). ✓
- §3.3 badge del capturista → Task 6. ✓
- §3.4 lectura para COORDINADOR, escritura vetada → Task 4 (split de guards + test). ✓
- §5 Testing (validaciones, scope, coordinador read/write, no fuga de clave) → tests en Tasks 2/3/4; Golden Rule #9 cubierta por `test_capture_cycle_and_consent` existente (clave nunca en claro). ✓
- Migración sigue reglas Alembic (aditiva, idempotente, SQLite-safe) → Task 1. ✓

**Placeholder scan:** sin TBD/TODO; todo paso con código lleva el código. ✓

**Type consistency:** `listMisRegistros(scope, q)` con misma firma en api (Task 5) y call sites (Tasks 5/6); `activista_nombre` idéntico en modelo/schema/TS; `scope: "mine"|"team"` consistente entre router (`Literal`), service (`str`) y frontend. `showActivista` definido y usado en `PersonRowProps`. ✓
