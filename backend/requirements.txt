# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Data / ORM ---
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3
geoalchemy2==0.16.0

# --- Validation / settings ---
pydantic==2.10.4
pydantic-settings==2.7.0
email-validator==2.2.0

# --- Security ---
# bcrypt is pinned: passlib 1.7.4's backend detection breaks with bcrypt>=4.1
# ("password cannot be longer than 72 bytes" at hash time). 4.0.1 is the last
# fully-compatible release.
passlib==1.7.4
bcrypt==4.0.1
pyjwt==2.10.1
python-multipart==0.0.20

# --- HTTP client (INE integrations + TestClient) ---
httpx==0.28.1

# --- Testing ---
pytest==8.3.4
